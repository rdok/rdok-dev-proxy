# rdok-dev-proxy
Docker based reverse proxy server providing domain proxying, SSL creation/reniewal/termination. 

## Upgrade
```
docker-compose stop
docker-compose rm -f
docker-compose pull   
docker-compose up -d
```
